# Web Vite client — same Convex deployment URL as apps/mobile EXPO_PUBLIC_CONVEX_URL (and `convex dev` for this repo).
# Convex MCP `status` / dashboard → Development: quixotic-kudu-10. Use Production URL only if you intentionally point web at prod.
VITE_CONVEX_URL=https://quixotic-kudu-10.convex.cloud
