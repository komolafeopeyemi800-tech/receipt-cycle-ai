/**
 * Shared with mobile via monorepo path (same copy for web bundle).
 */
export {
  userFacingError,
  userFacingErrorFromUnknown,
  stripTechnicalNoise,
} from "../../apps/mobile/src/lib/userFacingErrors";
